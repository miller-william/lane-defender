// Canvas dimensions
export const CANVAS_WIDTH = 600;
export const CANVAS_HEIGHT = 800;

// Player properties
export const PLAYER_WIDTH = 60;
export const PLAYER_HEIGHT = 80;
export const PLAYER_SPEED = 7; // pixels per frame (not time-based)

// Bullet properties
export const BULLET_WIDTH = 4;
export const BULLET_HEIGHT = 10;
export const BULLET_SPEED = 0.5; // canvas height per second (time-based)

// Enemy properties
export const ENEMY_RADIUS = 20;
export const ENEMY_SPEED = 0.8; // canvas height per second (time-based)

// Default dynamic game variables
export const DEFAULT_BULLET_DAMAGE = 1; // damage per bullet hit
export const DEFAULT_BULLET_FIRE_RATE = 500; // milliseconds between shots
export const DEFAULT_ENEMY_HEALTH = 3; // health points per enemy 